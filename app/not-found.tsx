import { NotFoundScreen } from "@/components/layout/not-found-screen";

export const metadata = { title: "Lens — page not found" };

export default function NotFound() {
  return <NotFoundScreen />;
}
